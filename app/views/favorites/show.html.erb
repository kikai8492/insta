<h1>お気に入り一覧</h1>
<% if @favorite_list.present?%>
    <% @favorite_list.each do | favorite |%>
    <p>いいねした投稿:<%= link_to favorite.content, blog_path(favorite.id) %></p>
    <% end %>
<%else%>
    <p>いいねした投稿はありません</p>
<%end%>

<%= link_to "戻る", blogs_path%>